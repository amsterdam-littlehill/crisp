# Shared Skills

> 跨 skill 通用约定放置处。
> 当项目安装了多个 skill 时，共享的规则、工具链配置放在这里。

## 使用方式

在单个 skill 的 SKILL.md 中引用：

```markdown
## Always Read
1. `rules/project-rules.md`
2. ../shared/toolchain-rules.md   ← 引用共享规则（单 skill 项目无需此项）
```

<!-- FILL: 多 skill 项目才需要填充。单 skill 项目可留空。 -->
